import Ember from 'ember'
import layout from '../templates/components/admin-resource-list'

const {
  Component
} = Ember

export default Component.extend({
  layout,
  classNames: ['admin-resource-list']
})

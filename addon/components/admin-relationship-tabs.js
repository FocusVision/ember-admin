import Ember from 'ember'
import layout from '../templates/components/admin-relationship-tabs'

const {
  Component
} = Ember

export default Ember.Component.extend({
  layout,
  classNames: ['admin-relationship-tabs']
})

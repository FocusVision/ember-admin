import Ember from 'ember'
import layout from '../templates/components/admin-relationship-list'

const {
  Component
} = Ember

export default Component.extend({
  layout,
  classNames: ['admin-relationship-list admin-resource-list']
})

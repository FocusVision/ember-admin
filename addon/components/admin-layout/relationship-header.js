import Ember from 'ember'
import layout from '../../templates/components/admin-layout/relationship-header'

const {
  Component
} = Ember

export default Component.extend({
  layout,
  classNames: ['admin-relationship-header']
})

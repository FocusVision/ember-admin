import Ember from 'ember'
import layout from '../../templates/components/admin-layout/page-header'

const {
  Component
} = Ember

export default Component.extend({
  layout,
  classNames: ['admin-page-header']
})

import Ember from 'ember'
import layout from '../../templates/components/admin-layout/main'

const {
  Component
} = Ember

export default Component.extend({
  layout,
  classNames: ['admin-layout-main']
})

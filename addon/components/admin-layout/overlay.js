import Ember from 'ember'
import layout from '../../templates/components/admin-layout/overlay'

const {
  Component
} = Ember

export default Component.extend({
  layout,
  classNames: ['admin-overlay']
})

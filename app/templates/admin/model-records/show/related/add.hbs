{{#admin-layout/overlay
  close=(action 'complete')}}

  {{admin-relationship-select
    model=model
    parentModel=parentModel
    recordType=recordType
    filterKeyword=filterKeywords
    filter=(action 'onFilterChange')
    add=(action 'add')
  }}

{{/admin-layout/overlay}}

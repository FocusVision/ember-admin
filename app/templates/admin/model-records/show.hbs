{{admin-form
  model=model
  recordType=recordType
  save=(action 'save')
  delete=(action 'delete')
  cancel=(action 'cancel')
  onUpdate=(action 'onFieldUpdate')
}}

{{#admin-layout/tabs as |tabs|}}
  {{#each relationships as |relationship|}}
    {{#tabs.tab}}
      {{#link-to
        'admin.model-records.show.related'
        relationship.key
      }}
        {{relationship.key}}
      {{/link-to}}
    {{/tabs.tab}}
  {{/each}}
{{/admin-layout/tabs}}

{{outlet}}

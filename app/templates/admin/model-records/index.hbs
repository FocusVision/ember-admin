{{admin-layout/page-header
  recordType=recordType
  filterKeyword=filterKeyword
  onFilterChange=(action 'onFilterChange')
}}

{{fv-paginator
  class='fv-pull-right'
  current=page
  total=pageCount
  surrounding=1
}}

{{admin-resource-list
  recordType=recordType
  model=model
  onRowClick=(action 'view')
}}
